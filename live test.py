nums=[11,7,2,15]



class solution:
    def countElements(self,nums:List[int])
    M=max(nums)
    m=min(nums)
    return sum(1 for in nums if m<i<M)


groups =[[1,-1,-1],[3,-2,0]],  nums = [1,-1,0,1,-1,-1,3,-2,0]
class Solution:
    def aniket(self,groups:List[list(int)],nums:List[int])
    def biuld(groups):
        n=len(group)
        lps=[0]*n
        j=0
        for i in range(1,n):
            while j>0 and group[i] !=group[j]:
                j=lps(j-1)
                if group[i]==group[j]:
                    lps(i)
                    j+=1
                    
                    return lps 
                def anky(groupindex,numIndex):
                    if groupIndex ==n:
                        return True
                    if numIndex ==m:
                        return False
                 
            
        